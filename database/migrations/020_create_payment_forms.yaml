version: "1.0"
name: create_payment_forms
description: "Formas de pago: Contado o Crédito"

up:
  - type: create_table
    table: payment_forms
    columns:
      - name: id
        type: SERIAL
        primary_key: true
      - name: code
        type: VARCHAR(5)
        nullable: false
        unique: true
      - name: name
        type: VARCHAR(50)
        nullable: false
      - name: description
        type: TEXT
      - name: is_active
        type: BOOLEAN
        default: true
        nullable: false
      - name: created_at
        type: TIMESTAMPTZ
        default: NOW()
        nullable: false
      - name: updated_at
        type: TIMESTAMPTZ
        default: NOW()
        nullable: false
    
    indexes:
      - name: idx_payment_forms_code
        columns: [code]
        where: "is_active = true"
    
    comment: "Formas de pago según DIAN: 1=Contado, 2=Crédito"

down:
  - type: drop_table
    table: payment_forms
    cascade: true
