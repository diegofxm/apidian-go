version: "1.0"
name: create_events
description: "Eventos DIAN para ApplicationResponse"

up:
  - type: create_table
    table: events
    columns:
      - name: id
        type: SERIAL
        primary_key: true
      - name: code
        type: VARCHAR(5)
        nullable: false
        unique: true
      - name: name
        type: VARCHAR(100)
        nullable: false
      - name: description
        type: TEXT
      - name: is_active
        type: BOOLEAN
        default: true
        nullable: false
      - name: created_at
        type: TIMESTAMPTZ
        default: NOW()
        nullable: false
      - name: updated_at
        type: TIMESTAMPTZ
        default: NOW()
        nullable: false
    
    indexes:
      - name: idx_events_code
        columns: [code]
        where: "is_active = true"
    
    comment: "Eventos DIAN para ApplicationResponse (Acuse, Aceptación, Rechazo)"

down:
  - type: drop_table
    table: events
    cascade: true
