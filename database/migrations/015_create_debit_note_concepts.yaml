version: "1.0"
name: create_debit_note_concepts
description: "Conceptos de nota débito según DIAN"

up:
  - type: create_table
    table: debit_note_concepts
    columns:
      - name: id
        type: SERIAL
        primary_key: true
      - name: code
        type: VARCHAR(5)
        nullable: false
        unique: true
      - name: name
        type: VARCHAR(255)
        nullable: false
      - name: description
        type: TEXT
      - name: is_active
        type: BOOLEAN
        default: true
        nullable: false
      - name: created_at
        type: TIMESTAMPTZ
        default: NOW()
        nullable: false
      - name: updated_at
        type: TIMESTAMPTZ
        default: NOW()
        nullable: false
    
    indexes:
      - name: idx_debit_note_concepts_code
        columns: [code]
        where: "is_active = true"
    
    comment: "Conceptos de nota débito DIAN (DiscrepancyResponse en UBL)"

down:
  - type: drop_table
    table: debit_note_concepts
    cascade: true
