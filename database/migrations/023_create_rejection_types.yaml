version: "1.0"
name: create_rejection_types
description: "Tipos de rechazo de documentos electrónicos"

up:
  - type: create_table
    table: rejection_types
    columns:
      - name: id
        type: SERIAL
        primary_key: true
      - name: code
        type: VARCHAR(5)
        nullable: false
        unique: true
      - name: name
        type: VARCHAR(255)
        nullable: false
      - name: description
        type: TEXT
      - name: is_active
        type: BOOLEAN
        default: true
        nullable: false
      - name: created_at
        type: TIMESTAMPTZ
        default: NOW()
        nullable: false
      - name: updated_at
        type: TIMESTAMPTZ
        default: NOW()
        nullable: false
    
    indexes:
      - name: idx_rejection_types_code
        columns: [code]
        where: "is_active = true"
    
    comment: "Tipos de rechazo de documentos electrónicos"

down:
  - type: drop_table
    table: rejection_types
    cascade: true
